# GridTanks (Backend)

From the backend repo root, start the API with:

```bash
python run_backend.py
```

Optional examples:

```bash
python run_backend.py --port 8080
python run_backend.py --no-reload
```
